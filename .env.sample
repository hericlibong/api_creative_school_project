📌 **Version épurée et prête pour un projet démo simple.** ✅  

---

### **📌 2️⃣ `.env.sample` (Simplifié)**
📍 **Crée un fichier `.env.sample`** et ajoute ceci :

```ini
# Clé secrète Django (changer pour la production si nécessaire)
SECRET_KEY="change_this_secret_key"

# Mode Debug
DEBUG=True

# Authentification JWT
ACCESS_TOKEN_LIFETIME=1  # Durée du token en jours
REFRESH_TOKEN_LIFETIME=7  # Durée du token refresh en jours
